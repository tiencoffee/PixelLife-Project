function use(path, {before = "", after = ""} = {}) {
	var vm = require("vm"),
		fs = require("fs"),
		code = before + fs.readFileSync("./" + path + ".js", "utf8") + after;

	vm.runInThisContext(code);

	return code;
}

module.exports = use;
