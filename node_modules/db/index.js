var fs = require("fs");

var db = {
	read(name) {
		return JSON.parse(fs.readFileSync("./models/data/" + name + ".json", "utf8"));
	},
	write(name, data) {
		fs.writeFile("./models/data/" + name + ".json", JSON.stringify(data));
	}
};

module.exports = db;
